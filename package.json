{
  "name": "aws-tunnel-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "verify:version": "node scripts/verify-version.mjs",
    "check:repository": "node scripts/check-repository.mjs",
    "check": "npm run verify:version && npm run check:repository && npm test && npm run build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@tauri-apps/api": "^2.11.1",
    "@vitejs/plugin-react": "6.0.5",
    "animejs": "^4.5.0",
    "motion": "^13.0.0",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "vite": "8.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
